@extends('layouts.saas')
@section('title', 'Campaigns · InfiMal')
@section('content')
<div class="flex items-center justify-between"><div><h1 class="text-2xl font-bold">Campaigns</h1><p class="text-sm text-slate-500">All campaign data is scoped to your account.</p></div><a href="{{ route('campaigns.create') }}" class="rounded-xl bg-blue-600 px-4 py-3 font-semibold text-white">New campaign</a></div>
<div class="grid gap-4 md:grid-cols-4">@foreach(['Total'=>$totalCampaigns,'Sent'=>$sentCampaigns,'Drafts'=>$draftCampaigns,'Scheduled'=>$scheduledCampaigns] as $label=>$value)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-bold">{{ $value }}</p></div>@endforeach</div>
<div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr class="text-left text-slate-500"><th class="py-2">Campaign</th><th>List</th><th>Status</th><th>Sent</th><th></th></tr></thead><tbody>@forelse($campaigns as $campaign)<tr class="border-t border-slate-200 dark:border-slate-800"><td class="py-3"><div class="font-semibold">{{ $campaign->name }}</div><div class="text-slate-500">{{ $campaign->subject }}</div></td><td>{{ $campaign->mailingList?->name ?? '—' }}</td><td>{{ $campaign->status }}</td><td>{{ $campaign->total_sent }}</td><td class="text-right"><a class="text-blue-600" href="{{ route('campaigns.show', $campaign) }}">View</a></td></tr>@empty<tr><td colspan="5" class="py-4 text-slate-500">No campaigns found.</td></tr>@endforelse</tbody></table></div></div>
@endsection
